# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
dist/
dist-server/
dist-ssr/
build/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env.test

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/
.tmp/

# Vite
.vite/

# Local Netlify folder
.netlify

# AI specific
.claude/
.cursor/
.roo/
.taskmaster/
.cline/
.windsurf/
.serena/
CLAUDE.md
.mcp.json
.gemini/

# Database files
*.db
*.sqlite
*.sqlite3 

logs
dev-debug.log
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# OS specific

# 不要再把 `tasks.json` / `tasks/` 加回来。
#
# 它们当初是为了忽略 TaskMaster 的状态（`.taskmaster/tasks/tasks.json`），
# 而那东西上面那条 `.taskmaster/` 已经全覆盖了 —— 所以这两条对它们
# 本来的目的**一点用也没有**，只剩下副作用：不带前导斜杠的模式在
# **任意深度**命中。
#
# 同一个坑踩过两次：
#   1. `tasks.json` 吞掉了 i18n 的 `tasks` 翻译命名空间，zh-TW 整篇翻译从没
#      提交进去，clone 的人拿到的是英文 UI；当时补了一条负向规则。
#   2. `tasks/` 吞掉了**整个定时任务功能实现** —— `server/modules/tasks/`
#      五个文件加 `src/components/tasks/TasksPage.tsx`，从头到尾没进过任何一次
#      提交。clone 下来就是一个没有定时任务、而且构建不过的 Prism，全程
#      没有任何东西报错。
#
# 第一次的负向规则只堆上了已知的那一个洞，拦不住第二个。真正的修法是
# 把规则本身去掉：没有这两行，就永远不会有第三个。

# Git worktrees
.worktrees/

# Local desktop packaging artifacts
/.desktop-build/
/release/
/electron/server-bundle-config.json
cloudcli-sidebar-app-source.tar.gz
cloudcli-sidebar.html
electron/*.tar.gz

# 部署/同步时"删掉"的文件的去处。
#
# 挂载盘和部分服务器环境不允许直接 rm,所以移除一个功能时统一 `mv` 到这里,
# 确认没问题后由人手动删掉整个目录。两个理由必须忽略它:
#
# · 它就在仓库根目录下,不忽略的话下一次打全量包会把整棵"已删除的树"一起打进去,
#   而那正是 tar 覆盖式部署最怕的东西 —— 死掉的 .ts 又回到服务器上;
# · 它按定义装的都是**不该再被任何工具看到**的文件。
#
# (tsc 那边是安全的:server/tsconfig.json 的 include 相对 server/,
#  根目录下的 _to_delete 本来就不在编译范围内。这条是给打包和 git 用的。)
/_to_delete/
